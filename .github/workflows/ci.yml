name: CI

on:
  push:
    branches:
      - main
      - "sprint/**"
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner starts
        run: echo "GitHub Actions runner is available"
